{% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}
<div class="row">
        <div class="col-7">
                <div class="ml-3" style="width: 460px;">
                         <hr>
                        {{ form_start(form) }}
                          {{ form_row(form.username,{'label':'Nom d\'utilsateur','attr':{'placeholder':'Nom utilisateur...'}}) }}
                          {{ form_row(form.email,{'label':'adresse email','attr':{'placeholder':'Adresse email...'}})}}
                          {{ form_row(form.role)}} 
                          {{ form_row(form.password,{'label':'Mot de passe','attr':{'placeholder':'Mot de passe...'}}) }}
                          {{ form_row(form.confirm,{'label':'Confirm MP','attr':{'placeholder':'Confirmer le mot de passe...'}}) }}
                          <hr class="my-4">
                             <input type="submit" class="btn btn-success" value="{{button_label}}" />
                      {{ form_end(form) }}
                      </div>
        </div>
        <div class="container-fluid col-4 my-3">
                <div class="card">
                        <div class="card-header">Liens rapides</div>
                        <div class="card-body">
                           
                         <li> <a href="{{ path('user_index') }}">Retour a la liste</a></li>
                         <li><a href="{{ path('user_new') }}"> Nouveau</a></li> 
                         <li><a href="{{ path('user_index') }}">Comportements</a></li>
                        <li> <a href="{{ path('user_index') }}">Laisser un message</a></li>
                        <li><a href="{{ path('user_index') }}">Retour a la liste</a></li>
                
                        </div>
                      </div>
        </div>
</div>

